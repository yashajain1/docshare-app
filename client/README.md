# Client README

This client is a minimal React + Vite app. After installing dependencies, run:

cd client
npm install
npm run dev

The client expects the backend at http://localhost:8000 and will call API endpoints using X-User-Id header for mock auth.
